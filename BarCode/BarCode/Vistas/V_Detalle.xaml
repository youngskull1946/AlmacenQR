<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BarCode.Vistas.V_Detalle">
    <ContentPage.Content>
        <StackLayout BackgroundColor="WhiteSmoke" Margin="10,20,10,10">
            <Label Text="Lista de Herramientas"
                HorizontalOptions="Center" FontSize="Title" />
            <ListView x:Name="ListaRefacciones" BackgroundColor="#96CCB7">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Id}" Detail="{Binding Nombre}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>